stages:
  - lint
  - build

variables:
  DOCKER_AUTH_CONFIG: '{"auths": {"artifactory.pegadaian.co.id:8084": {"auth": "$DOCKER_AU_CONFIG"}}}'
  IMAGE_URL: artifactory.pegadaian.co.id:5443/$CI_PROJECT_NAME:$CI_COMMIT_REF_SLUG
  PACKAGE_PATH: /go/src/gade/srv-goldcard

lint:
  image:
    name: artifactory.pegadaian.co.id:8084/dian/golangci:v1.23.1
    entrypoint: [""]
  stage: lint
  script:
    - go mod download
    - golangci-lint run
  only:
    - merge_requests

build:
  stage: build
  image: artifactory.pegadaian.co.id:8084/docker:latest
  services:
    - name: artifactory.pegadaian.co.id:8084/docker:dind
      command: ["--insecure-registry=artifactory.pegadaian.co.id:8084"]
  script:
    - docker login -u $NEXUS_DOCKER_USER -p $NEXUS_DOCKER_PASS artifactory.pegadaian.co.id:8084
    - docker build --pull -t $IMAGE_URL .
    - docker push $IMAGE_URL
